export type MaskImageLoaderState = {
    cache: Map<string, Promise<ImageBitmap>>;
    images: Set<ImageBitmap>;
    signal: AbortSignal | null;
    timeoutInMilliseconds: number;
    [Symbol.dispose]: () => void;
};
export declare const makeMaskImageLoaderState: ({ signal, timeoutInMilliseconds, }: {
    signal: AbortSignal | null;
    timeoutInMilliseconds: number;
}) => MaskImageLoaderState;
export declare const getMaskImage: ({ src, state, }: {
    src: string;
    state: MaskImageLoaderState;
}) => Promise<ImageBitmap>;
