export declare const getBackgroundImageRect: ({ backgroundPosition, backgroundSize, positioningArea, }: {
    backgroundPosition: string;
    backgroundSize: string;
    positioningArea: DOMRect;
}) => DOMRect;
