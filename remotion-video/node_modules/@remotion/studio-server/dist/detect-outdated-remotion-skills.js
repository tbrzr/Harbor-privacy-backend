"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.detectOutdatedRemotionSkills = exports.parseRemotionSkillVersion = void 0;
const node_fs_1 = require("node:fs");
const node_os_1 = require("node:os");
const node_path_1 = __importDefault(require("node:path"));
const version_1 = require("remotion/version");
const semver_1 = __importDefault(require("semver"));
const remotion_skill_names_1 = require("./remotion-skill-names");
const parseRemotionSkillVersion = (contents) => {
    var _a, _b, _c, _d;
    var _e;
    const frontmatter = (_b = (_a = contents.match(/^---\r?\n(?<frontmatter>[\s\S]*?)\r?\n---(?:\r?\n|$)/)) === null || _a === void 0 ? void 0 : _a.groups) === null || _b === void 0 ? void 0 : _b.frontmatter;
    if (!frontmatter) {
        return null;
    }
    return ((_e = (_d = (_c = frontmatter.match(/^version:\s*(?<quote>['"]?)(?<version>[^\s'"]+)\k<quote>\s*$/m)) === null || _c === void 0 ? void 0 : _c.groups) === null || _d === void 0 ? void 0 : _d.version) !== null && _e !== void 0 ? _e : null);
};
exports.parseRemotionSkillVersion = parseRemotionSkillVersion;
const readRemotionSkill = (skillFile) => {
    if (!(0, node_fs_1.existsSync)(skillFile)) {
        return { installed: false, version: null };
    }
    try {
        return {
            installed: true,
            version: (0, exports.parseRemotionSkillVersion)((0, node_fs_1.readFileSync)(skillFile, 'utf8')),
        };
    }
    catch (_a) {
        return { installed: true, version: null };
    }
};
const getStatusForSkillsDirectory = ({ currentVersion, scope, skillsDirectory, }) => {
    const installedSkills = new Map();
    for (const skillName of remotion_skill_names_1.remotionSkillNames) {
        installedSkills.set(skillName, readRemotionSkill(node_path_1.default.join(skillsDirectory, skillName, 'SKILL.md')));
    }
    if ([...installedSkills.values()].every((skill) => !skill.installed)) {
        return { type: 'not-installed', scope, skillsDirectory };
    }
    const outdatedSkillNames = remotion_skill_names_1.remotionSkillNames.filter((skillName) => {
        const skill = installedSkills.get(skillName);
        return (!(skill === null || skill === void 0 ? void 0 : skill.installed) ||
            skill.version === null ||
            semver_1.default.valid(skill.version) === null ||
            semver_1.default.lt(skill.version, currentVersion));
    });
    if (outdatedSkillNames.length > 0) {
        return { type: 'outdated', scope, skillsDirectory, outdatedSkillNames };
    }
    return { type: 'up-to-date', scope, skillsDirectory };
};
const detectOutdatedRemotionSkills = ({ currentVersion = version_1.VERSION, cwd = process.cwd(), homeDirectory = (0, node_os_1.homedir)(), } = {}) => {
    const projectSkillsDirectory = node_path_1.default.join(cwd, '.agents', 'skills');
    const globalSkillsDirectory = node_path_1.default.join(homeDirectory, '.agents', 'skills');
    return {
        project: getStatusForSkillsDirectory({
            currentVersion,
            scope: 'project',
            skillsDirectory: projectSkillsDirectory,
        }),
        global: getStatusForSkillsDirectory({
            currentVersion,
            scope: 'global',
            skillsDirectory: globalSkillsDirectory,
        }),
    };
};
exports.detectOutdatedRemotionSkills = detectOutdatedRemotionSkills;
