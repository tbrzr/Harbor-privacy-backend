"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveEditor = void 0;
const renderer_1 = require("@remotion/renderer");
const custom_editor_1 = require("./custom-editor");
const editor_registry_1 = require("./editor-registry");
const open_in_editor_1 = require("./open-in-editor");
let legacyEditors = null;
const getLegacyEditors = () => {
    legacyEditors !== null && legacyEditors !== void 0 ? legacyEditors : (legacyEditors = (0, open_in_editor_1.guessEditor)());
    return legacyEditors;
};
const warnedEditors = new Set();
const defaultDependencies = {
    getInstalledEditors: editor_registry_1.getAvailableEditors,
    getLegacyEditors,
    resolveCustomEditor: custom_editor_1.resolveCustomEditorExecutable,
    warn: () => undefined,
    warnedEditors,
};
const resolveEditor = async ({ defaultEditor, logLevel, preferredEditor, }, overrides = {}) => {
    var _a, _b;
    const dependencies = {
        ...defaultDependencies,
        warn: (message) => {
            renderer_1.RenderInternals.Log.warn({ indent: false, logLevel }, message);
        },
        ...overrides,
    };
    if (preferredEditor === 'custom') {
        if (!defaultEditor || typeof defaultEditor !== 'object') {
            return null;
        }
        const resolvedExecutable = dependencies.resolveCustomEditor(defaultEditor);
        if (!resolvedExecutable) {
            return null;
        }
        return {
            ...resolvedExecutable,
            type: 'custom',
            id: 'custom',
            name: defaultEditor.name,
            nameWithType: defaultEditor.name,
            editor: defaultEditor,
        };
    }
    if (preferredEditor !== null) {
        const installedEditors = await dependencies.getInstalledEditors();
        const installedEditor = installedEditors.find((editor) => editor.id === preferredEditor);
        return installedEditor ? { ...installedEditor, type: 'built-in' } : null;
    }
    if (defaultEditor && typeof defaultEditor === 'object') {
        const resolvedExecutable = dependencies.resolveCustomEditor(defaultEditor);
        if (resolvedExecutable) {
            return {
                ...resolvedExecutable,
                type: 'custom',
                id: 'custom',
                name: defaultEditor.name,
                nameWithType: defaultEditor.name,
                editor: defaultEditor,
            };
        }
        const warningId = `custom:${defaultEditor.executable}`;
        if (!dependencies.warnedEditors.has(warningId)) {
            dependencies.warnedEditors.add(warningId);
            dependencies.warn(`The executable for custom editor ${defaultEditor.name} (${defaultEditor.executable}) was not found or is not executable. Falling back to automatic editor detection.`);
        }
    }
    if (typeof defaultEditor === 'string') {
        const installedEditors = await dependencies.getInstalledEditors();
        const installedEditor = installedEditors.find((editor) => editor.id === defaultEditor);
        if (installedEditor) {
            return { ...installedEditor, type: 'built-in' };
        }
        if (!dependencies.warnedEditors.has(defaultEditor)) {
            dependencies.warnedEditors.add(defaultEditor);
            dependencies.warn(`The default editor ${(0, editor_registry_1.getDefaultEditorName)(defaultEditor)} (${defaultEditor}) is not installed. Falling back to automatic editor detection.`);
        }
    }
    const [legacyEditor] = await dependencies.getLegacyEditors();
    if (!legacyEditor) {
        return null;
    }
    return {
        ...legacyEditor,
        type: 'built-in',
        id: null,
        name: (_a = (0, open_in_editor_1.getDisplayNameForEditor)(legacyEditor.command)) !== null && _a !== void 0 ? _a : legacyEditor.command,
        nameWithType: (_b = (0, open_in_editor_1.getDisplayNameForEditor)(legacyEditor.command)) !== null && _b !== void 0 ? _b : legacyEditor.command,
    };
};
exports.resolveEditor = resolveEditor;
