import type { TerminalId } from '@remotion/studio-shared';
type SupportedTerminalPlatform = 'darwin' | 'linux' | 'win32';
export type InstalledTerminal = {
    id: TerminalId;
    name: string;
    applicationPath: string;
    platform: SupportedTerminalPlatform;
};
export type TerminalDiscoveryContext = {
    platform: NodeJS.Platform;
    env: NodeJS.ProcessEnv;
    homeDirectory: string;
    pathExists: (filePath: string) => boolean;
    findMacApplications: (bundleIdentifier: string) => Promise<readonly string[]>;
};
export declare const discoverAvailableTerminals: (context: TerminalDiscoveryContext) => Promise<readonly InstalledTerminal[]>;
export declare const getAvailableTerminals: () => Promise<readonly InstalledTerminal[]>;
type TerminalLaunchInstruction = {
    type: 'command';
    command: string;
    args: string[];
    cwd: string;
} | {
    type: 'url';
    url: string;
};
export declare const getTerminalLaunchInstruction: ({ terminal, directory, }: {
    terminal: InstalledTerminal;
    directory: string;
}) => TerminalLaunchInstruction;
export declare const launchTerminal: ({ terminal, directory, allowedDirectory, }: {
    terminal: InstalledTerminal;
    directory: string;
    allowedDirectory: string;
}) => Promise<void>;
export {};
