import type { BuiltInEditor } from '@remotion/renderer';
export type InstalledEditor = {
    id: BuiltInEditor;
    name: string;
    nameWithType: string;
    process: string;
    command: string;
};
export type EditorDiscoveryContext = {
    platform: NodeJS.Platform;
    env: NodeJS.ProcessEnv;
    homeDirectory: string;
    pathExists: (filePath: string) => boolean;
    findMacApplications: (bundleIdentifier: string) => Promise<readonly string[]>;
    findWindowsApplications: (editor: BuiltInEditor) => Promise<readonly string[]>;
};
export declare const getDefaultEditorName: (id: "cursor" | "sublime-text" | "vscode" | "vscodium" | "webstorm" | "windsurf" | "zed") => string;
export declare const discoverAvailableEditors: (context: EditorDiscoveryContext) => Promise<readonly InstalledEditor[]>;
export declare const getAvailableEditors: () => Promise<readonly InstalledEditor[]>;
