// src/studio-entry-points.ts
var getStudioEntryPoints = ({
  fastRefreshRuntime,
  environmentSetup,
  sequenceStackTraces,
  userDefinedComponent,
  reactShim,
  studioRenderEntry
}) => [
  fastRefreshRuntime,
  environmentSetup,
  sequenceStackTraces,
  userDefinedComponent,
  reactShim,
  studioRenderEntry
].filter(Boolean);
export {
  getStudioEntryPoints
};
